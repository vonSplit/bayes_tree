{
    "version": "2.0.0",
    "tasks": [
        // build your whole project before launching the debugger.
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build build --config Debug",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
